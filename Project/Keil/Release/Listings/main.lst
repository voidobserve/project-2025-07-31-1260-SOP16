C51 COMPILER V9.60.7.0   MAIN                                                              07/31/2025 10:50:55 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE MAIN
OBJECT MODULE PLACED IN .\Release\Objects\main.obj
COMPILER INVOKED BY: C:\Keil_v5\C51\BIN\C51.EXE ..\..\User\main.c LARGE OPTIMIZE(8,SPEED) BROWSE INTVECTOR(0X000C) INCDI
                    -R(..\..\Libraries\Include;..\..\User) INTERVAL(3) DEBUG OBJECTEXTEND PRINT(.\Release\Listings\main.lst) OBJECT(.\Release
                    -\Objects\main.obj)

line level    source

   1          /**
   2           ******************************************************************************
   3           * @file    main.c
   4           * @author  HUGE-IC Application Team
   5           * @version V1.0.0
   6           * @date    02-09-2022
   7           * @brief   Main program body
   8           ******************************************************************************
   9           * @attention
  10           *
  11           * <h2><center>&copy; COPYRIGHT 2021 HUGE-IC</center></h2>
  12           *
  13           * 版权说明后续补上
  14           *
  15           ******************************************************************************
  16           */
  17          
  18          /* Includes ------------------------------------------------------------------*/
  19          #include "my_config.h"
  20          #include "include.h"
  21          #include <math.h>
  22          #include <stdio.h>
  23          
  24          // float step = 70;
  25          // float mi; // 幂
  26          // float rus; // 10的幂次方
  27          // float r_ms = 0;
  28          // #define USER_BAUD (115200UL)
  29          // #define USER_UART_BAUD ((SYSCLK - USER_BAUD) / (USER_BAUD))
  30          
  31          volatile float32 time_accumulate = 0.0;
  32              
  33          
  34          #if USE_MY_DEBUG // 打印串口配置
              
              #define UART0_BAUD 115200
              #define USER_UART0_BAUD ((SYSCLK - UART0_BAUD) / (UART0_BAUD))
              // 重写puchar()函数
              char putchar(char c)
              {
                  while (!(UART0_STA & UART_TX_DONE(0x01)))
                      ;
                  UART0_DATA = c;
                  return c;
              }
              
              void my_debug_config(void)
              {
                  // 作为发送引脚
                  P1_MD0 &= (GPIO_P13_MODE_SEL(0x3));
                  P1_MD0 |= GPIO_P13_MODE_SEL(0x1);            // 配置为输出模式
                  FOUT_S13 |= GPIO_FOUT_UART0_TX;              // 配置为UART0_TX
                  UART0_BAUD1 = (USER_UART0_BAUD >> 8) & 0xFF; // 配置波特率高八位
C51 COMPILER V9.60.7.0   MAIN                                                              07/31/2025 10:50:55 PAGE 2   

                  UART0_BAUD0 = USER_UART0_BAUD & 0xFF;        // 配置波特率低八位
                  UART0_CON0 = UART_STOP_BIT(0x0) |
                               UART_EN(0x1); // 8bit数据，1bit停止位
              }
              #endif // USE_MY_DEBUG // 打印串口配置
  59          
  60          // 开机缓启动，调节占空比：
  61          void adjust_pwm_duty_when_power_on(void)
  62          {
  63   1      #if 0
                  if (c_duty < 6000)
                  {
                      /* step初始值为70 */
                      mi = (step - 1) / (253 / 3) - 1;
                      step += 0.5;
                      /*
                          C 库函数 double pow(double x, double y) 返回 x 的 y 次幂
                          原本是 c_duty = pow(5, mi) * 60，实际测试开机8s左右
                      */
                      c_duty = pow(5, mi) * 60;
                  }
              
                  if (c_duty >= 6000)
                  {
                      c_duty = 6000;
                  }
              
                  // printf("c_duty %d\n",c_duty);
                  printf("%d\n", c_duty);
              
                  // delay_ms(16); // 每16ms调整一次PWM的脉冲宽度 ---- 校验码A488对应的时间
                  // delay_ms(11); // 16 * 0.666 约为10.656   ---- 校验码B5E3对应的时间
              #endif
  87   1      
  88   1      
  89   1      
  90   1          if (c_duty < PWM_DUTY_50_PERCENT)
  91   1          {
  92   2      // 持续时间 40s，每13.3ms变化1
  93   2      #define STEP_1 ((float32)40000 / PWM_DUTY_50_PERCENT)
  94   2              if (time_accumulate >= STEP_1)
  95   2              {
  96   3                  time_accumulate -= STEP_1;
  97   3                  c_duty++;
  98   3              }
  99   2          }
 100   1          else if (c_duty < PWM_DUTY_100_PERCENT)
 101   1          {
 102   2      // 持续时间20s，每 6.66ms变化1
 103   2      #define STEP_2 ((float32)20000 / (PWM_DUTY_100_PERCENT - PWM_DUTY_50_PERCENT))
 104   2              if (time_accumulate >= STEP_2)
 105   2              {
 106   3                  time_accumulate -= STEP_2;
 107   3                  c_duty++;
 108   3              }
 109   2          }
 110   1          else
 111   1          {
 112   2              c_duty = PWM_DUTY_100_PERCENT;
 113   2          }
 114   1      
 115   1          // printf("%u\n", c_duty);
C51 COMPILER V9.60.7.0   MAIN                                                              07/31/2025 10:50:55 PAGE 3   

 116   1      }
 117          
 118          void main(void)
 119          {
 120   1          // 看门狗默认打开, 复位时间2s
 121   1          WDT_KEY = WDT_KEY_VAL(0xDD); //  关闭看门狗 (如需配置看门狗请查看“WDT\WDT_Reset”示
             -例)
 122   1      
 123   1          system_init();
 124   1      
 125   1      #if USE_MY_DEBUG // 打印串口配置
                  // 初始化打印
                  my_debug_config();
              
                  // 输出模式：
                  // P1_MD0 &= (GPIO_P13_MODE_SEL(0x3));
                  // P1_MD0 |= GPIO_P13_MODE_SEL(0x1); // 配置为输出模式
                  // FOUT_S13 = GPIO_FOUT_AF_FUNC;     // 选择AF功能输出
              #endif // 打印串口配置
 134   1      
 135   1          // 过压保护  16脚-----P14
 136   1          //          P1_MD1   &= ~GPIO_P14_MODE_SEL(0x03);
 137   1          //          P1_MD1   |=  GPIO_P14_MODE_SEL(0x01);
 138   1          //          FOUT_S14  =  GPIO_FOUT_AF_FUNC;
 139   1          ///////////////////////////////////////////
 140   1      
 141   1      #if 1
 142   1          adc_pin_config(); // 配置使用到adc的引脚
 143   1          // adc_sel_pin(ADC_SEL_PIN_GET_TEMP);
 144   1          tmr0_config(); // 配置定时器，默认关闭
 145   1          pwm_init();    // 配置pwm输出的引脚
 146   1          tmr1_config();
 147   1      
 148   1          timer2_config();
 149   1      #endif
 150   1      
 151   1          adc_sel_pin(ADC_SEL_PIN_GET_VOL); // 切换到9脚，准备检测9脚的电压
 152   1      
 153   1      // ===================================================================
 154   1      #if 1        // 开机缓慢启动（PWM信号变化平缓）
 155   1          P14 = 0; // 16脚先输出低电平
 156   1          c_duty = 0;
 157   1          while (c_duty < 6000)
 158   1          {
 159   2              /*
 160   2                  原本计划在开机缓启动时检测发送机供电是否稳定，但是实际测试发现�
             -��易误检，导致提前退出
 161   2                  现在只有框架，哪怕检测到发送机供电不稳定，也不会有任何操作
 162   2              */
 163   2              adc_update_pin_9_adc_val(); // 采集并更新9脚的ad值
 164   2      
 165   2      #if USE_MY_DEBUG // 直接打印0，防止在串口+图像上看到错位
                               // printf(",b=0,");  // 防止错位
              
              #endif
 169   2      
 170   2              if (flag_is_pwm_sub_time_comes) // pwm递减时间到来
 171   2              {
 172   3                  flag_is_pwm_sub_time_comes = 0;
 173   3      
 174   3                  if (adc_val_pin_9 >= ADC_VAL_WHEN_UNSTABLE) // 只要有一次跳动，退出开机缓启动
 175   3                  {
C51 COMPILER V9.60.7.0   MAIN                                                              07/31/2025 10:50:55 PAGE 4   

 176   4                      // if (c_duty >= PWM_DUTY_50_PERCENT)
 177   4                      if (c_duty >= PWM_DUTY_100_PERCENT)
 178   4                      {
 179   5                          // adjust_duty = c_duty;
 180   5                          break;
 181   5                      }
 182   4                  }
 183   3              }
 184   2      
 185   2              // if (flag_time_comes_during_power_on) // 如果调节时间到来 -- 13ms
 186   2              if (flag_time_comes_during_power_on) // 如果调节时间到来 -- 1 ms
 187   2              {
 188   3                  flag_time_comes_during_power_on = 0;
 189   3                  adjust_pwm_duty_when_power_on();
 190   3              }
 191   2      
 192   2              set_pwm_duty(); // 将 c_duty 写入pwm对应的寄存器
 193   2      
 194   2      #if USE_MY_DEBUG
                      // printf("power_on_duty %u\n", c_duty);
              #endif //  USE_MY_DEBUG
 197   2          }
 198   1      #endif
 199   1          // ===================================================================
 200   1      
 201   1          while (1)
 202   1          {
 203   2      #if 1
 204   2              adc_update_pin_9_adc_val(); // 采集并更新9脚的ad值
 205   2              temperature_scan();         // 检测热敏电阻一端的电压值
 206   2      
 207   2              set_duty();                       // 设定到要调节到的脉宽
 208   2              according_pin9_to_adjust_pin16(); // 根据9脚的电压来设定16脚的电平
 209   2      
 210   2      #if USE_MY_DEBUG
                      // printf("adjust_duty %u\n", adjust_duty);
                      // printf(",b=%u,", adjust_duty);
              #endif //  USE_MY_DEBUG
 214   2      
 215   2      #endif
 216   2      
 217   2              // P13 = ~P13;
 218   2          }
 219   1      }
 220          
 221          /**
 222           * @}
 223           */
 224          
 225          /*************************** (C) COPYRIGHT 2022 HUGE-IC ***** END OF FILE *****/


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    302    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =      4    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
